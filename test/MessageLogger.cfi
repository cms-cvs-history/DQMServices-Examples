service = MessageLogger
{
   untracked vstring destinations =
   {
	"MessageLogger",
	"cout",
	"cerr"
   }

   untracked vstring fwkJobReports = 
   {
	"FrameworkJobReport"
   }

   untracked vstring categories = 
   { 
	"FwkJob",
	"MEtoEDMConverter_MEtoEDMConverter",
	"MEtoEDMConverter_endJob",
	"MEtoEDMConverter_beginRun",
	"MEtoEDMConverter_endRun",	
	"EDMtoMEConverter_EDMtoMEConverter",
	"EDMtoMEConverter_endJob",
	"EDMtoMEConverter_beginRun",
	"EDMtoMEConverter_endRun",
	"ConverterTester_ConverterTester",
	"ConverterTester_endJob",
	"ConverterTester_beginRun",
	"ConverterTester_endRun",
	"PostConverterAnalyzer_PostConverterAnalyzer",
	"PostConverterAnalyzer_endJob",
	"PostConverterAnalyzer_beginRun",
	"PostConverterAnalyzer_endRun",
	"ConverterQualityTester_ConverterQualityTester",
	"ConverterQualityTester_endJob",
	"ConverterQualityTester_beginRun",
	"ConverterQualityTester_endRun",
	"ScheduleExecutionFailure",
	"EventSetupDependency",
	"Root_Warning",
	"Root_Error"
    }

   untracked PSet MessageLogger =
   {
      untracked int32 lineLength = 132
      untracked bool noLineBreaks = true
      untracked PSet default = { untracked int32 limit = 0  }
      untracked PSet MEtoEDMConverter_MEtoEDMConverter = 
        { untracked int32 limit = 10000000 }
      untracked PSet MEtoEDMConverter_endJob = 
        { untracked int32 limit = 10000000 }
      untracked PSet MEtoEDMConverter_beginRun = 
        { untracked int32 limit = 10000000 }
      untracked PSet MEtoEDMConverter_endRun = 
        { untracked int32 limit = 10000000 }
      untracked PSet EDMtoMEConverter_EDMtoMEConverter = 
        { untracked int32 limit = 10000000 }
      untracked PSet EDMtoMEConverter_endJob = 
        { untracked int32 limit = 10000000 }
      untracked PSet EDMtoMEConverter_beginRun = 
        { untracked int32 limit = 10000000 }
      untracked PSet EDMtoMEConverter_endRun = 
        { untracked int32 limit = 10000000 }
      untracked PSet ConverterTester_ConverterTester = 
        { untracked int32 limit = 10000000 }
      untracked PSet ConverterTester_endJob = 
        { untracked int32 limit = 10000000 }
      untracked PSet ConverterTester_beginRun = 
        { untracked int32 limit = 10000000 }
      untracked PSet ConverterTester_endRun = 
        { untracked int32 limit = 10000000 }
      untracked PSet PostConverterAnalyzer_PostConverterAnalyzer = 
        { untracked int32 limit = 10000000 }
      untracked PSet PostConverterAnalyzer_endJob = 
        { untracked int32 limit = 10000000 }
      untracked PSet PostConverterAnalyzer_beginRun = 
        { untracked int32 limit = 10000000 }
      untracked PSet PostConverterAnalyzer_endRun = 
        { untracked int32 limit = 10000000 }
      untracked PSet ConverterQualityTester_ConverterQualityTester = 
        { untracked int32 limit = 10000000 }
      untracked PSet ConverterQualityTester_endJob = 
        { untracked int32 limit = 10000000 }
      untracked PSet ConverterQualityTester_beginRun = 
        { untracked int32 limit = 10000000 }
      untracked PSet ConverterQualityTester_endRun = 
        { untracked int32 limit = 10000000 }
      untracked PSet ScheduleExecutionFailure = 
        { untracked int32 limit = 10000000 }
      untracked PSet EventSetupDependency = 
        { untracked int32 limit = 10000000 }
      untracked PSet Root_Warning =
        { untracked int32 limit = 10000000 }
      untracked PSet Root_Error =
        { untracked int32 limit = 10000000 }
    }

   untracked PSet cout =
   {
      untracked int32 lineLength = 132
      untracked bool noLineBreaks = true
      untracked string threshold = "INFO"
      untracked PSet default = { untracked int32 limit = 0  }
      untracked PSet MEtoEDMConverter_MEtoEDMConverter = 
        { untracked int32 limit = 10000000 }
      untracked PSet MEtoEDMConverter_endJob = 
        { untracked int32 limit = 10000000 }
      untracked PSet MEtoEDMConverter_beginRun = 
        { untracked int32 limit = 10000000 }
      untracked PSet MEtoEDMConverter_endRun = 
        { untracked int32 limit = 10000000 }
      untracked PSet EDMtoMEConverter_EDMtoMEConverter = 
        { untracked int32 limit = 10000000 }
      untracked PSet EDMtoMEConverter_endJob = 
        { untracked int32 limit = 10000000 }
      untracked PSet EDMtoMEConverter_beginRun = 
        { untracked int32 limit = 10000000 }
      untracked PSet EDMtoMEConverter_endRun = 
        { untracked int32 limit = 10000000 }
      untracked PSet ConverterTester_ConverterTester = 
        { untracked int32 limit = 10000000 }
      untracked PSet ConverterTester_endJob = 
        { untracked int32 limit = 10000000 }
      untracked PSet ConverterTester_beginRun = 
        { untracked int32 limit = 10000000 }
      untracked PSet ConverterTester_endRun = 
        { untracked int32 limit = 10000000 }
      untracked PSet PostConverterAnalyzer_PostConverterAnalyzer = 
        { untracked int32 limit = 10000000 }
      untracked PSet PostConverterAnalyzer_endJob = 
        { untracked int32 limit = 10000000 }
      untracked PSet PostConverterAnalyzer_beginRun = 
        { untracked int32 limit = 10000000 }
      untracked PSet PostConverterAnalyzer_endRun = 
        { untracked int32 limit = 10000000 }
      untracked PSet ConverterQualityTester_ConverterQualityTester = 
        { untracked int32 limit = 10000000 }
      untracked PSet ConverterQualityTester_endJob = 
        { untracked int32 limit = 10000000 }
      untracked PSet ConverterQualityTester_beginRun = 
        { untracked int32 limit = 10000000 }
      untracked PSet ConverterQualityTester_endRun = 
        { untracked int32 limit = 10000000 }
    }

   untracked PSet cerr =
   {
      untracked int32 lineLength = 132
      untracked bool noLineBreaks = true
      untracked string threshold = "WARNING"
      untracked PSet default = { untracked int32 limit = 0  }
      untracked PSet MEtoEDMConverter_MEtoEDMConverter = 
        { untracked int32 limit = 10000000 }
      untracked PSet MEtoEDMConverter_endJob = 
        { untracked int32 limit = 10000000 }
      untracked PSet MEtoEDMConverter_beginRun = 
        { untracked int32 limit = 10000000 }
      untracked PSet MEtoEDMConverter_endRun = 
        { untracked int32 limit = 10000000 }
      untracked PSet EDMtoMEConverter_EDMtoMEConverter = 
        { untracked int32 limit = 10000000 }
      untracked PSet EDMtoMEConverter_endJob = 
        { untracked int32 limit = 10000000 }
      untracked PSet EDMtoMEConverter_beginRun = 
        { untracked int32 limit = 10000000 }
      untracked PSet EDMtoMEConverter_endRun = 
        { untracked int32 limit = 10000000 }
      untracked PSet ConverterTester_ConverterTester = 
        { untracked int32 limit = 10000000 }
      untracked PSet ConverterTester_endJob = 
        { untracked int32 limit = 10000000 }
      untracked PSet ConverterTester_beginRun = 
        { untracked int32 limit = 10000000 }
      untracked PSet ConverterTester_endRun = 
        { untracked int32 limit = 10000000 }
      untracked PSet PostConverterAnalyzer_PostConverterAnalyzer = 
        { untracked int32 limit = 10000000 }
      untracked PSet PostConverterAnalyzer_endJob = 
        { untracked int32 limit = 10000000 }
      untracked PSet PostConverterAnalyzer_beginRun = 
        { untracked int32 limit = 10000000 }
      untracked PSet PostConverterAnalyzer_endRun = 
        { untracked int32 limit = 10000000 }
      untracked PSet ConverterQualityTester_ConverterQualityTester = 
        { untracked int32 limit = 10000000 }
      untracked PSet ConverterQualityTester_endJob = 
        { untracked int32 limit = 10000000 }
      untracked PSet ConverterQualityTester_beginRun = 
        { untracked int32 limit = 10000000 }
      untracked PSet ConverterQualityTester_endRun = 
        { untracked int32 limit = 10000000 }
      untracked PSet ScheduleExecutionFailure = 
        { untracked int32 limit = 10000000 }
      untracked PSet EventSetupDependency = 
        { untracked int32 limit = 10000000 }
      untracked PSet Root_Warning =
        { untracked int32 limit = 10000000 }
      untracked PSet Root_Error =
        { untracked int32 limit = 10000000 }
    }

   untracked PSet FrameworkJobReport = 
   {
      //untracked PSet default = 
      //        { untracked int32 limit = 0 }
      untracked PSet FwkJob = 
        { untracked int32 limit = 10000000 }
   }
}